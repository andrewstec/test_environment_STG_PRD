name: Deploy to Production

on:
  workflow_dispatch: 

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    environment: production
    needs: deploy-staging

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to Production
        run: |
          echo "Deploying to PRD... ONLY IF STG deployment done."

      - name: Install maven repo and authorize
        run: |
          echo "Installing Java, mvn, and configuring step... "
          echo "MY_MVN_INSTALL" > maven.install.mock

      - name: Viewing mvn install
        run: |
          cat maven.install.mock

